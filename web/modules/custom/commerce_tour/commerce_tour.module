<?php

/**
 * @file
 * This module only installs config, so the module file is empty.
 */

function m4_garbage_function($unused_param) {
  $var = 7;
     $thing = "test";
     
$var += 17;

while($var < 20){ $var++ };
// commening thingggs!!


}
